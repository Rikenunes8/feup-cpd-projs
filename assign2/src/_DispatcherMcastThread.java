public class _DispatcherMcastThread implements Runnable{
    private Store store;

    public _DispatcherMcastThread(Store store) {
        this.store = store;
    }

    @Override
    public void run() {

    }
}
